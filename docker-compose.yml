services:
  ecommerce:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - ecommerce_db:/data/db
      - ecommerce_uploads:/data/uploads

volumes:
  ecommerce_db:
  ecommerce_uploads:
